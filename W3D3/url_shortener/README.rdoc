== =^.^=
This app takes long url inputs from a user, generates a unique 16 character code and stores it in a DB alongside the original URL.
You can then feed the code into the application and watch it open up your earlier inputted url.
